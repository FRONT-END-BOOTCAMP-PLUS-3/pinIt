import styles from '../addLocation.module.scss';
const AddressSection: React.FC = () => {
  return <div className={styles.addressSectionContainer}>AddressSection</div>;
};

export default AddressSection;
